Fix: screens rendering pinned to the top-right instead of centered

Context

The two screenshots show every screen's main container (the translucent card
holding the heading + buttons/content) rendered off to the right edge instead
of centered — on the Main Menu it's shifted right, and on Chat History the
whole "expand" card collapses into a narrow strip pinned to the top-right
corner, with the background showing through on the left.

This is a regression from the uncommitted, in-progress change to
background_screen() in src/views/common/engine/screen.py (the "static BG
across navigation via ft.Hero" feature being built — see the WIP diff and
the new TestPageBackgroundStaysFixed tests in tests/test_screen_shell.py).
That change wrapped the screen's foreground conte
(to layer it above the BG ft.Hero):

ft.Container(expand=True, content=ft.Stack(expand
    ft.Hero(tag=..., content=ft.Container(expand=True, ...)),   # BG
    ft.Container(expand=True, content=content),                  # foreground
]))

The bug: Flet's expand flag only has an effect when the control's direct
parent is a Column/Row/View/Page (documented on C
flet/controls/control.py). A Stack is none of those, so
ft.Container(expand=True, content=content) does not fill the stack —
it collapses to its content's intrinsic size. ft.
non-positioned child at its default alignment (No
AlignmentDirectional.topStart), and since page.rtl = True
(src/app.py:74), topStart resolves to the visual top-right corner —
exactly what the screenshots show.

This silently introduced a second place that decides how the main container
is positioned, fighting the existing, single sour
in src/views/_base.py:256, which is the one funct
to center every screen's card (_compose_frame → _
centered ft.Container(alignment=Alignment(0,0)) s
a ft.Column(horizontal_alignment=CENTER) for expand cards). The user wants
that to remain the only place centering is decided — so the fix is to make
background_screen a fully transparent passthrough
centering logic anywhere else.

Confirmed: this affects every screen, not just the three already spotted

The user found the off-center rendering on Chat H
Chat (החיש) and My Profile (ילש ליפורפ) — plus th
the first screenshot. I checked whether any other screen could be exempt:

- All 13 concrete screens (login_view, signup_vie
system_views/placeholder_view, chat_view, discover_view,
matches_view, main_menu_view, additional_photos_view,
my_profile_view, peer_photos_view, user_profile_v
BaseView and rely on its build().
- None of them override _assemble, _compose_frame
_compact_card, _expand_card, or build — BaseView is, per its own
docs, "the single home of structural-layout decisions."
- background_screen(...) is invoked from exactly
whole codebase: BaseView._assemble (src/views/_ba

So every screen funnels through the same background_screen → same ft.Stack
→ same missing fit. The bug isn't per-view — it's
the one-line fit=ft.StackFit.EXPAND fix below cor
uniformly (the three the user already found, the Main Menu, and every other
screen in the app alike).

Approach

Make the new ft.Stack give its non-positioned children the same tight,
full-size constraints the old single ft.Container
wrapper gave content directly — restoring the exa
chain that _wrap_card's centering already depends on. Flet exposes this via
Stack.fit:

▎ StackFit.EXPAND — "The constraints passed to th
▎ tightened to the biggest size allowed... the no
▎ stack would all be sized as [the full] pixels wide and high."

So the fix is a one-line addition in background_s
(src/views/common/engine/screen.py, the ft.Stack(

ft.Stack(
    expand=True,
    fit=ft.StackFit.EXPAND,     # ← force BOTH la
    controls=[...],
)

With fit=EXPAND:
- the BG Hero/Container layer fills the screen exactly as it did as a bare
expand=True container before (full-bleed background preserved), and
- the foreground ft.Container(expand=True, conten
to the full viewport and passes those tight const
content (the _wrap_card output) — identical to th
where content sat directly inside ft.Container(ex

No changes are needed in _base.py / _wrap_card — it already is, and
remains, the single function that decides how the main container (card +
buttons/content) is centered. This fix simply rem
positioning layer that background_screen's new Stack introduced, restoring
background_screen to its documented role as a transparent BG wrapper.

Also tighten the background_screen docstring/comm
fit=ft.StackFit.EXPAND is required (the expand-on
View/Page rule + RTL topStart = visual top-right)
function doesn't reintroduce the same silent regression.

Files to change

- src/views/common/engine/screen.py — background_screen() (~line 53-79):
add fit=ft.StackFit.EXPAND to the ft.Stack(...) and extend the comment
block above _PAGE_BG_HERO_TAG / the function docs
constraint-propagation requirement.

Verification

No new/changed tests — the existing 106 tests in
test_component_orchestration.py already pass and
(e.g. that responsive_card_of resolves the tagged
centering shell is present); they're a fine regression net as-is and don't
need touching for a one-line Stack.fit change.

The user will launch the app and visually re-check the two screens from the
screenshots once the change is in:
- Main Menu (/menu): the card with "ישאר טירפת" +
should be horizontally centered (not shifted righ
- Chat History (/matching/chat-history): the expand-card should
fill/center in the viewport at its clamped width — not collapse into a
strip pinned to the top-right with background showing on the left.
- Navigating between routes (e.g. Menu → Chat His
show the original WIP goal holding: the BG image
across the transition while the foreground card slides/fades and ends up
centered.